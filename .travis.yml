language: node_js
node_js: 'lts/*'
install:
  - npm install now -g
script:
  - yarn
  - yarn run build
  - yarn run deploy
  - yarn run alias
  - yarn run unalias
if: branch = master AND type IN (push)
